import { FloatButton } from 'antd';
import { IconShoppingCart } from '@tabler/icons-react';

const Hello = () => (
        <div>
            <h1>Hello</h1>
        </div>
    );

export default Hello;
