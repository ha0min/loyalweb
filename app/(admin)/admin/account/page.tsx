'use client';

import { PageContainer } from '@ant-design/pro-layout';

const Page = () => (
    <PageContainer
        header={{
            title: 'Account',
            ghost: true,
            breadcrumb: {},
        }}
    >
            <h1>Account Page</h1>
    </PageContainer>
    );

export default Page;
